@using Components
@model Container

<div class="w-full flex justify-center">
    <div class="w-full max-w-screen-lg flex flex-row px-5 md:px-12 items-center">
        @Model.Content(null)
    </div>
</div>