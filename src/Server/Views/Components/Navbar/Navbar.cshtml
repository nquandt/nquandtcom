@using Components
@using Components.Buttons
@using Components.Utils
@model Navbar

<div class="py-3 flex w-full justify-between fixed dark:bg-black light:bg-light z-10"
    style="height: 80px;">
    @(await Html.RenderViewAsync(new Container()
{    Content = 
    @<div class="flex w-full h-full items-center justify-between" hx-target="#inner">
            @(await Html.RenderViewAsync(new HtmxATag("/", "quandt")))
            <div class="flex gap-4 items-center">
                @(await Html.RenderViewAsync(new HtmxATag("/resume", "Resume")))
                @* @(await Html.RenderViewAsync(new HtmxATag("/blog", "Blog"))) *@
                @(await Html.RenderViewAsync<ThemeButton>())
                @* <ThemeButton /> *@
            </div>
        </div>    
    }))    
</div>
<div class="py-3 flex w-full justify-between" style="min-height: 80px;"></div>

